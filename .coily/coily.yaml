# Per-repo command allowlist. Add verbs here as they emerge.

commands:
  warp:
    run: go run -C warp .
    description: "Establish and verify Kai's Warp config across hosts. `coily exec warp apply` renders all three state layers (repo template, config-dir TOML, SQLite); `coily exec warp doctor` reports drift. See project-a-coily-exec-warp.md."
  apply-agentic-os-hooks:
    run: python3 scripts/apply-agentic-os-hooks.py
    description: "Roll out the canonical pre-commit hook block to every coilysiren/* consumer repo. Idempotent."
  audit-pre-commit-coverage:
    run: python3 scripts/audit-pre-commit-coverage.py
    description: "Audit every coilysiren/* repo for presence of the canonical agentic-os hook block."
  sweep-documentation-layout:
    run: python3 scripts/sweep-documentation-layout.py
    description: "Read-only sweep: run the documentation-layout check across every workspace git repo and report passing/failing/disabled plus per-repo violation detail."
  sweep-precommit:
    run: python3 scripts/sweep-precommit.py
    description: "Run the full pre-commit hook set (pre-commit run --all-files) across every workspace git repo and report per-repo Passed/Failed plus failing hook ids. Reverts autofix mutations so trees stay clean."
  pre-commit-all:
    run: pre-commit run --all-files
    description: "Run all pre-commit hooks against all files in the current repo."
  release:
    run: python3 scripts/release.py
    description: "Cut a release: parse conventional commits since latest tag, bump pyproject + DEFAULT_REV, create signed tag, push. --bump major|minor|patch to override; --dry-run to preview."

# Catalog metadata for the cross-repo knowledge graph.
# Schema: coilysiren/agentic-os-kai#420 (tracker).
catalog:
  kind: Component
  type: configuration
  system: ops
  owner: kai
  lifecycle: production
  description: "Personal agentic OS: shell config, terminal config, GPG/SSM tooling, Claude skills, and shared utility scripts symlinked into sibling coilysiren/* repos. Growing beyond just dotfiles into broader personal-OS scope."
  dependsOn: []
