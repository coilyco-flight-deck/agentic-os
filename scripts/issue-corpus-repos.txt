# Source set for the issue-corpus renderer (scripts/render-issue-corpus.py): the
# repos whose Forgejo issues (open AND closed, bodies + every comment) render to
# the private discovery-index mirror. A discovery index, not a source of truth -
# grep it to locate an issue, then confirm live via `ward ops forgejo issue view`.
#
# This is the corpus source list, deliberately distinct from the public image
# seed (docker/dev-base/substrate-image-repos.txt): it ADDS the private
# coilysiren/inbox, which must never land in the public substrate image. When a
# Forgejo-hosted substrate repo is added there, mirror it here by hand.
#
# Every entry must be Forgejo-hosted, since the renderer speaks only `ward ops
# forgejo`. GitHub-origin substrate repos (coilysiren/coilysiren) are omitted
# until their Forgejo mirror lands (infrastructure#352); add them here then.
#
# owner/name, one per line.
coilyco-flight-deck/agentic-os
coilyco-flight-deck/cli-guard
coilyco-flight-deck/infrastructure
coilyco-flight-deck/ward
coilysiren/inbox
