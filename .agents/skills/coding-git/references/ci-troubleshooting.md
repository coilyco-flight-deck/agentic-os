# CI Troubleshooting Quick Reference

Common CI failure patterns and how to diagnose them from the logs. Split into two reference pages so each stays under the doc caps.

- [CI failure patterns](ci-failure-patterns.md) - reading logs plus test, lint, and type-check failures.
- [CI build and infra failures](ci-build-infra-failures.md) - build, permission, timeout, and container failures, the auto-fix decision tree, and re-running after a fix.
