# Image-tier substrate repos: public reference repos baked into the dev-base
# image as bare-mirror seeds at /opt/substrate-seed, so a ward container on a
# cold gitcache volume warms them with no network. The entrypoint hydrates the
# gitcache from these seeds (see ward cmd/ward/containerassets/entrypoint.sh).
#
# This is a denormalized copy of the image-tier subset of ward's canonical list
# (ward cmd/ward/containerassets/preclone-repos.txt). The bridge/private
# (cache-tier) repos are deliberately NOT baked into this shareable image.
#
# owner/name, one per line. All are public read (anonymous clone). coilysiren is
# GitHub-origin until its Forgejo mirror lands (infrastructure#352).
coilyco-flight-deck/agentic-os
coilyco-flight-deck/cli-guard
coilyco-flight-deck/infrastructure
coilyco-flight-deck/ward
coilysiren/coilysiren
